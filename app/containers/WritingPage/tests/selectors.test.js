// import { fromJS } from 'immutable';
// import { makeSelectWritingPageDomain } from '../selectors';

// const selector = makeSelectWritingPageDomain();

describe('makeSelectWritingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
